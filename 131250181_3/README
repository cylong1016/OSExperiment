1. 实验说明参照：操作系统实验三.pdf
2. 直接在终端输入 make run 即可运行
3. 如果出现下面的错误：
-------------------------------------------------------
umount：/mnt/floppy：设备忙。
        (有些情况下在 lsof(8) 或 fuser(1)) 中可以找到
         有关使用该设备的进程的有用信息
make: *** [buildimg] 错误 1
-------------------------------------------------------
请输入命令： sudo umount /mnt/floppy 运行两次【请不要问我为什么，我也不知道】

